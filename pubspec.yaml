name: dandi_bot
description: "Dandi Bot - app educativo de analise e simulacao de ativos financeiros."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.12.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  fl_chart: ^0.68.0
  go_router: ^13.2.5
  http: ^1.2.2
  intl: ^0.19.0
  shimmer: ^3.0.0
  uuid: ^4.5.1
  web: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
