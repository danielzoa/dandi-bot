# Codex Notes

- Quando o Daniel pedir uma alteracao no projeto, implementar, verificar, criar commit apenas com os arquivos alterados pela tarefa e enviar para `origin/master` no GitHub, preservando mudancas soltas que ja existirem no workspace.
- Para qualquer alteracao no Flutter Web ou em `functions/`, tambem gerar o build, publicar no projeto Cloudflare Pages `dandi-bot` na branch `master` e verificar `https://dandi-bot.pages.dev/`. So nao publicar quando Daniel pedir explicitamente.
